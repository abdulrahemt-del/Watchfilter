import { MarketIntelligencePulse } from "@/components/MarketIntelligencePulse";

export const metadata = { title: "Market Intelligence Hub — WatchFilter" };

export default function IntelligencePage() {
  return <MarketIntelligencePulse />;
}
